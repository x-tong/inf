# AGENTS.md

本文件是 AI coding agent 在本仓库工作的入口规则。任何自动化修改都应先阅读本文件和 `rules/` 目录。

## 工作方式

- 先理解上下文，再修改文件。
- 优先阅读 `README.md`、`docs/README.md` 和相关项目文档。
- 修改前说明将要做什么，修改后说明改了什么、如何验证。
- 不要覆盖用户未明确要求修改的内容。
- 不要做与当前任务无关的重构、格式化或文件移动。
- 新增重要设计、规则或约定时，同时更新相关 docs/rules 索引。

## 提交规则

- commit message 使用中文。
- commit 应聚焦一个主题，避免把无关改动混在一起。
- 提交前检查 `git status`，确认没有误加入临时文件、密钥、缓存或大文件。
- 推荐格式：

```text
动词 + 对象 + 目的

例如：
初始化 vibecoding 仓库框架
补充 SLO-aware runtime 项目规划
修正 benchmark 文档结构
```

## 文档规则

- `docs/` 存放项目设计、路线图、ADR、实验和 benchmark 报告。
- `rules/` 存放长期有效的协作和工程规则。
- 设计文档应包含背景、目标、非目标、方案、验证方式和风险。
- benchmark 结论必须保留数据来源、运行环境和复现命令。

## 代码规则

- 代码结构应服务于清晰边界，避免过早抽象。
- 新增实现时优先加入最小可验证测试或 benchmark。
- 性能优化必须有 baseline 和对照数据。
- 不提交生成物、缓存、密钥、依赖目录和本地 IDE 状态。

